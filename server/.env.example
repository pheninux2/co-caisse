# ============================================================
# Co-Caisse — Variables d'environnement SERVEUR
# Copier ce fichier en .env et adapter les valeurs
# NE PAS committer le .env réel dans git
# ============================================================

# ── Base de données MariaDB ──────────────────────────────────
DB_HOST=localhost
DB_PORT=3306
DB_USER=cocaisse
DB_PASS=cocaisse
DB_NAME=cocaisse

# ── JWT ──────────────────────────────────────────────────────
JWT_SECRET=changeme_super_secret_jwt_key_min_32_chars
JWT_EXPIRES_IN=8h

# ── Serveur Express ───────────────────────────────────────────
PORT=5000

# ── CORS ─────────────────────────────────────────────────────
# Origines autorisées, séparées par des virgules
# Dev   : http://localhost:3000
# Prod  : http://192.168.1.X:3000
ALLOWED_ORIGIN=http://localhost:3000

# ── Conformité NF525 — Chaînage fiscal anti-fraude TVA ────────────────────────
# Obligatoire si fiscal_chain_enabled=1 dans les paramètres de l'application
# Générer via : node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
FISCAL_HMAC_KEY=changeme_fiscal_hmac_key_min_32_chars_NF525

# ── Email (nodemailer) — Ticket dématérialisé AGEC ────────────────────────────
# Loi AGEC (août 2023) : le ticket papier n'est plus imprimé par défaut en France.
# Renseigner un serveur SMTP pour activer l'envoi de tickets par email.
#
# Exemples fournisseurs :
#   Gmail       : SMTP_HOST=smtp.gmail.com  SMTP_PORT=587
#   Mailtrap    : SMTP_HOST=sandbox.smtp.mailtrap.io  SMTP_PORT=2525  (tests uniquement)
#   OVH         : SMTP_HOST=ssl0.ovh.net  SMTP_PORT=465
#   Mailgun     : SMTP_HOST=smtp.mailgun.org  SMTP_PORT=587
#
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=votre_email@example.com
SMTP_PASS=votre_mot_de_passe_smtp
# Expéditeur affiché dans le client mail du client
SMTP_FROM="Co-Caisse" <noreply@votre-domaine.fr>


