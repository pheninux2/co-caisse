# ====================================================
# Co-Caisse — Variables Docker
# Copie ce fichier en .env à la racine du projet
# Ne jamais commiter le .env réel !
# ====================================================

# MariaDB — Root (interne Docker uniquement)
DB_ROOT_PASSWORD=Sherine2011$

# MariaDB — Utilisateur applicatif
DB_HOST=localhost
DB_PORT=3306
DB_USER=cocaisse
DB_PASS=cocaisse
DB_NAME=cocaisse
